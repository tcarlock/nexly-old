<% if @business.active_platforms.empty? %>
	<li class="no-records">You haven't enabled any sites</li>
<% else %>
	<% @business.active_platforms.each do |p| %>
		<li>
			<%= p.display_name %> is enabled
		</li>
	<% end %>
<% end %>