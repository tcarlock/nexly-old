var li = $('#biz-review-<%= params[:id] %>');

li.find('span.featured').fadeIn();
li.find('div.btns-tab').html('<%= escape_javascript(render(:partial => "reviews/admin_buttons", :locals => { :review => @review })) %>');
